# HTTP/WebSocket port used by server.ts.
PORT=3000

# Set true in constrained environments to disable Vite HMR and file watching.
DISABLE_HMR=false

# Production mode serves the prebuilt dist/ directory instead of Vite middleware.
NODE_ENV=development
